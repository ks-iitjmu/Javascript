// Kunal Sharma IIT Jammu

function onePlusAvg(a, b) {
  return 1 + (a + b) / 2;
}
console.log("One Plus Average of 4 and 8 is", onePlusAvg(4, 8));
console.log("One Plus Average of 12 and 98 is", onePlusAvg(12, 98));
console.log("One Plus Average of 4908 and 3975 is", onePlusAvg(4908, 3975));

const hello = () => {
  console.log("Hey How are You?");
};
hello();

const sums = (a, b) => {
  return a + b;
};
console.log("Sum of 23 and 34 is", sums(23, 34));
