// Kunal Sharma IIT Jammu
let a = document.getElementsByTagName("div")[0];

// a.innerHTML = a.innerHTML + "<h1>THis is added using innerHTML in JS.</h1>";

let div = document.createElement("div");
div.innerHTML = "<h1>THis is added using createElement() in JS.</h1>";
// a.appendChild(div);
// a.append(div);
// a.prepend(div);
// a.before(div);
// a.after(div);
// a.replaceWith(div);
